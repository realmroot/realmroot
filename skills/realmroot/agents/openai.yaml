interface:
  display_name: "Realmroot"
  short_description: "Operate Realmroot and registered APIs"
  default_prompt: "Use $realmroot to operate Realmroot or a registered API with discovery-driven, least-privilege access."
policy:
  allow_implicit_invocation: true
