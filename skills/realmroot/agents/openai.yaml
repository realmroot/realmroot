interface:
  display_name: "Realmroot"
  short_description: "Operate Realmroot and Agent-authorized APIs"
  default_prompt: "Use $realmroot to establish my Agent identity and call a registered API Resource with Restish."
policy:
  allow_implicit_invocation: true
