diff --git a/dist/index.mjs b/dist/index.mjs
index 7091595..cfeeaf4 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -9,7 +9,7 @@ import { generateRandomString, makeSignature } from "better-auth/crypto";
 import { defineRequestState } from "@better-auth/core/context";
 import { logger } from "@better-auth/core/env";
 import { BetterAuthError } from "@better-auth/core/error";
-import { parseSetCookieHeader } from "better-auth/cookies";
+import { deleteSessionCookie, parseSetCookieHeader } from "better-auth/cookies";
 import { mergeSchema } from "better-auth/db";
 import * as z from "zod";
 import { signJWT, toExpJWT } from "better-auth/plugins";
@@ -262,6 +262,7 @@ async function userInfoEndpoint(ctx, opts) {
 }
 //#endregion
 //#region src/token.ts
+const deviceCodeGrantType = "urn:ietf:params:oauth:grant-type:device_code";
 /**
 * Handles the /oauth2/token endpoint by delegating
 * the grant types
@@ -276,6 +277,7 @@ async function tokenEndpoint(ctx, opts) {
 		case "authorization_code": return handleAuthorizationCodeGrant(ctx, opts);
 		case "client_credentials": return handleClientCredentialsGrant(ctx, opts);
 		case "refresh_token": return handleRefreshTokenGrant(ctx, opts);
+		case deviceCodeGrantType: return handleDeviceCodeGrant(ctx, opts);
 		case void 0: throw new APIError("BAD_REQUEST", {
 			error_description: "missing required grant_type",
 			error: "unsupported_grant_type"
@@ -297,18 +299,28 @@ async function createJwtAccessToken(ctx, opts, user, client, audience, scopes, r
 		metadata: parseClientMetadata(client.metadata)
 	}) : {};
 	const jwtPluginOptions = getJwtPlugin(ctx.context).options;
+	const applicationId = parseClientMetadata(client.metadata)?.applicationId;
 	return signJWT(ctx, {
-		options: jwtPluginOptions,
+		// TODO(better-auth-1.7): Remove this patch when upgrading; 1.7 sets the
+		// RFC 9068 access-token media type through a per-signing-call header.
+		options: {
+			...jwtPluginOptions,
+			jwt: {
+				...jwtPluginOptions?.jwt,
+				type: "at+jwt"
+			}
+		},
 		payload: {
 			...customClaims,
-			sub: user?.id,
+			sub: user?.id ?? (typeof applicationId === "string" ? applicationId : client.clientId),
 			aud: typeof audience === "string" ? audience : audience?.length === 1 ? audience.at(0) : audience,
-			azp: client.clientId,
+			client_id: client.clientId,
 			scope: scopes.join(" "),
 			sid: overrides?.sid,
 			iss: jwtPluginOptions?.jwt?.issuer ?? ctx.context.baseURL,
 			iat,
-			exp
+			exp,
+			jti: crypto.randomUUID()
 		}
 	});
 }
@@ -447,6 +459,6 @@ async function invalidateRefreshFamily(ctx, clientId, userId) {
 	});
 }
-async function createRefreshToken(ctx, opts, user, referenceId, client, scopes, payload, originalRefresh, authTime) {
+async function createRefreshToken(ctx, opts, user, referenceId, client, scopes, resources, payload, originalRefresh, authTime) {
 	const iat = payload.iat ?? Math.floor(Date.now() / 1e3);
 	const exp = payload?.exp ?? iat + (opts.refreshTokenExpiresIn ?? 2592e3);
 	const token = opts.generateRefreshToken ? await opts.generateRefreshToken() : generateRandomString(32, "A-Z", "a-z");
@@ -457,6 +469,7 @@ async function createRefreshToken(ctx, opts, user, referenceId, client, scopes,
 		referenceId,
 		authTime,
 		scopes,
+		resources,
 		createdAt: /* @__PURE__ */ new Date(iat * 1e3),
 		expiresAt: /* @__PURE__ */ new Date(exp * 1e3)
 	};
@@ -487,24 +500,60 @@ async function createRefreshToken(ctx, opts, user, referenceId, client, scopes,
 */
 async function checkResource(ctx, opts, scopes) {
 	const resource = ctx.body.resource;
-	const audience = typeof resource === "string" ? [resource] : resource ? [...resource] : void 0;
-	if (audience) {
-		if (scopes.includes("openid")) audience.push(`${ctx.context.baseURL}/oauth2/userinfo`);
-		const validAudiences = new Set([...opts.validAudiences ?? [ctx.context.baseURL], scopes?.includes("openid") ? `${ctx.context.baseURL}/oauth2/userinfo` : void 0].flat().filter((v) => v?.length));
-		for (const aud of audience) if (!validAudiences.has(aud)) throw new APIError("BAD_REQUEST", {
-			error_description: "requested resource invalid",
-			error: "invalid_request"
-		});
-	}
+	const userInfoAudience = `${ctx.context.baseURL}/oauth2/userinfo`;
+	const audience = typeof resource === "string" ? [resource] : resource ? [...resource] : [scopes.includes("openid") ? userInfoAudience : ctx.context.baseURL];
+	const validAudiences = new Set([...opts.validAudiences ?? [ctx.context.baseURL], userInfoAudience].flat().filter((v) => v?.length));
+	for (const aud of audience) if (!validAudiences.has(aud)) throw new APIError("BAD_REQUEST", {
+		error_description: "requested resource invalid",
+		error: "invalid_target"
+	});
 	return audience?.length === 1 ? audience.at(0) : audience;
 }
+function resourceValues(value) {
+	if (typeof value === "string") return [value];
+	if (Array.isArray(value)) return [...new Set(value)];
+	return [];
+}
+function selectAccessTokenResource(ctx, grantedResources) {
+	const requestedResources = resourceValues(ctx.body.resource);
+	if (requestedResources.length > 1) throw new APIError("BAD_REQUEST", {
+		error_description: "request one resource per access token",
+		error: "invalid_target"
+	});
+	if (grantedResources !== void 0) {
+		const granted = new Set(grantedResources);
+		for (const resource of requestedResources) if (!granted.has(resource)) throw new APIError("BAD_REQUEST", {
+			error_description: "requested resource is outside the original authorization grant",
+			error: "invalid_target"
+		});
+		if (!requestedResources.length && grantedResources.length > 1) throw new APIError("BAD_REQUEST", {
+			error_description: "resource is required for a multi-resource authorization grant",
+			error: "invalid_target"
+		});
+		if (!requestedResources.length && grantedResources.length === 1) requestedResources.push(grantedResources[0]);
+	}
+	ctx.body.resource = requestedResources[0];
+	return requestedResources[0];
+}
 async function createUserTokens(ctx, opts, params) {
-	const { client, scopes, user, grantType, referenceId, sessionId, nonce, refreshToken: existingRefreshToken, authTime, verificationValue } = params;
+	const { client, scopes: requestedScopes, user, grantType, referenceId, sessionId, nonce, refreshToken: existingRefreshToken, authTime, verificationValue } = params;
+	const grantedResources = verificationValue ? resourceValues(verificationValue.query.resource) : existingRefreshToken?.resources ?? void 0;
+	const resource = selectAccessTokenResource(ctx, grantedResources);
+	const filteredScopes = opts.filterAccessTokenScopes ? await opts.filterAccessTokenScopes({
+		user,
+		scopes: requestedScopes,
+		resource,
+		referenceId,
+		metadata: parseClientMetadata(client.metadata),
+		grantType
+	}) : requestedScopes;
+	const allowedScopes = new Set(filteredScopes);
+	const scopes = requestedScopes.filter((scope) => allowedScopes.has(scope));
 	const iat = Math.floor(Date.now() / 1e3);
 	const defaultExp = iat + (user ? opts.accessTokenExpiresIn ?? 3600 : opts.m2mAccessTokenExpiresIn ?? 3600);
 	const exp = opts.scopeExpirations ? scopes.map((sc) => opts.scopeExpirations?.[sc] ? toExpJWT(opts.scopeExpirations[sc], iat) : defaultExp).reduce((prev, curr) => {
 		return prev < curr ? prev : curr;
 	}, defaultExp) : defaultExp;
 	const audience = await checkResource(ctx, opts, scopes);
 	const isRefreshToken = user && clientAllowsGrant(client, "refresh_token") && (existingRefreshToken?.scopes?.includes("offline_access") || scopes.includes("offline_access"));
 	const isJwtAccessToken = audience && !opts.disableJwtPlugin;
@@ -516,8 +566,8 @@ async function createUserTokens(ctx, opts, params) {
 		scopes,
 		metadata: parseClientMetadata(client.metadata),
 		verificationValue
 	}) : void 0;
-	const earlyRefreshToken = isRefreshToken && user && !isJwtAccessToken ? await createRefreshToken(ctx, opts, user, referenceId, client, scopes, {
+	const earlyRefreshToken = isRefreshToken && user && !isJwtAccessToken ? await createRefreshToken(ctx, opts, user, referenceId, client, requestedScopes, grantedResources, {
 		iat,
 		exp: iat + (opts.refreshTokenExpiresIn ?? 2592e3),
 		sid: sessionId
@@ -531,7 +581,7 @@ async function createUserTokens(ctx, opts, params) {
 			exp,
 			sid: sessionId
 		}, referenceId, earlyRefreshToken?.id),
-		earlyRefreshToken ? earlyRefreshToken : isRefreshToken && user ? createRefreshToken(ctx, opts, user, referenceId, client, scopes, {
+		earlyRefreshToken ? earlyRefreshToken : isRefreshToken && user ? createRefreshToken(ctx, opts, user, referenceId, client, requestedScopes, grantedResources, {
 			iat,
 			exp: iat + (opts.refreshTokenExpiresIn ?? 2592e3),
 			sid: sessionId
@@ -682,6 +705,125 @@ async function handleAuthorizationCodeGrant(ctx, opts) {
 		verificationValue
 	});
 }
+async function handleDeviceCodeGrant(ctx, opts) {
+	let { client_id, client_secret, device_code } = ctx.body;
+	const authorization = ctx.request?.headers.get("authorization") || null;
+	if (authorization?.startsWith("Basic ")) {
+		const res = basicToClientCredentials(authorization);
+		client_id = res?.client_id;
+		client_secret = res?.client_secret;
+	}
+	if (!client_id) throw new APIError("BAD_REQUEST", {
+		error_description: "client_id is required",
+		error: "invalid_request"
+	});
+	if (!device_code) throw new APIError("BAD_REQUEST", {
+		error_description: "device_code is required",
+		error: "invalid_request"
+	});
+	const deviceCodeRecord = await ctx.context.adapter.findOne({
+		model: "deviceCode",
+		where: [{
+			field: "deviceCode",
+			value: device_code
+		}]
+	});
+	if (!deviceCodeRecord) throw new APIError("BAD_REQUEST", {
+		error_description: "Invalid device code",
+		error: "invalid_grant"
+	});
+	if (deviceCodeRecord.clientId && deviceCodeRecord.clientId !== client_id) throw new APIError("BAD_REQUEST", {
+		error_description: "Client ID mismatch",
+		error: "invalid_grant"
+	});
+	const scopes = (deviceCodeRecord.scope || "openid").split(/\s+/).filter(Boolean);
+	const client = await validateClientCredentials(ctx, opts, client_id, client_secret, scopes);
+	if (client.grantTypes && !client.grantTypes.includes(deviceCodeGrantType)) throw new APIError("BAD_REQUEST", {
+		error_description: `client does not allow grant type ${deviceCodeGrantType}`,
+		error: "invalid_client"
+	});
+	if (deviceCodeRecord.lastPolledAt && deviceCodeRecord.pollingInterval && Date.now() - new Date(deviceCodeRecord.lastPolledAt).getTime() < deviceCodeRecord.pollingInterval) throw new APIError("BAD_REQUEST", {
+		error_description: "Polling too frequently",
+		error: "slow_down"
+	});
+	await ctx.context.adapter.update({
+		model: "deviceCode",
+		where: [{
+			field: "id",
+			value: deviceCodeRecord.id
+		}],
+		update: { lastPolledAt: /* @__PURE__ */ new Date() }
+	});
+	if (deviceCodeRecord.expiresAt < /* @__PURE__ */ new Date()) {
+		await ctx.context.adapter.delete({
+			model: "deviceCode",
+			where: [{
+				field: "id",
+				value: deviceCodeRecord.id
+			}]
+		});
+		throw new APIError("BAD_REQUEST", {
+			error_description: "Device code has expired",
+			error: "expired_token"
+		});
+	}
+	if (deviceCodeRecord.status === "pending") throw new APIError("BAD_REQUEST", {
+		error_description: "Authorization pending",
+		error: "authorization_pending"
+	});
+	if (deviceCodeRecord.status === "denied") {
+		await ctx.context.adapter.delete({
+			model: "deviceCode",
+			where: [{
+				field: "id",
+				value: deviceCodeRecord.id
+			}]
+		});
+		throw new APIError("BAD_REQUEST", {
+			error_description: "Access denied",
+			error: "access_denied"
+		});
+	}
+	if (deviceCodeRecord.status === "approved" && deviceCodeRecord.userId) {
+		const user = await ctx.context.internalAdapter.findUserById(deviceCodeRecord.userId);
+		if (!user) throw new APIError("INTERNAL_SERVER_ERROR", {
+			error_description: "User not found",
+			error: "server_error"
+		});
+		const session = await ctx.context.internalAdapter.createSession(user.id);
+		if (!session) throw new APIError("INTERNAL_SERVER_ERROR", {
+			error_description: "Failed to create session",
+			error: "server_error"
+		});
+		ctx.context.setNewSession({
+			session,
+			user
+		});
+		if (ctx.context.options.secondaryStorage) await ctx.context.secondaryStorage?.set(session.token, JSON.stringify({
+			user,
+			session
+		}), Math.floor((new Date(session.expiresAt).getTime() - Date.now()) / 1e3));
+		await ctx.context.adapter.delete({
+			model: "deviceCode",
+			where: [{
+				field: "id",
+				value: deviceCodeRecord.id
+			}]
+		});
+		return createUserTokens(ctx, opts, {
+			client,
+			scopes,
+			user,
+			grantType: deviceCodeGrantType,
+			sessionId: session.id,
+			authTime: resolveSessionAuthTime(session)
+		});
+	}
+	throw new APIError("INTERNAL_SERVER_ERROR", {
+		error_description: "Invalid device code status",
+		error: "server_error"
+	});
+}
 /**
 * Grant that allows direct access to an API using the application's credentials
 * This grant is for M2M so the concept of a user id does not exist on the token.
@@ -771,13 +913,10 @@ async function handleRefreshTokenGrant(ctx, opts) {
 		error_description: "invalid refresh token",
 		error: "invalid_grant"
 	});
-	if (refreshToken.revoked) {
-		await invalidateRefreshFamily(ctx, client_id, refreshToken.userId);
-		throw new APIError("BAD_REQUEST", {
+	if (refreshToken.revoked) throw new APIError("BAD_REQUEST", {
 			error_description: "invalid refresh token",
 			error: "invalid_grant"
 		});
-	}
 	const scopes = refreshToken?.scopes;
 	const requestedScopes = scope?.split(" ");
 	if (requestedScopes) {
@@ -846,10 +985,11 @@ async function validateJwtAccessToken(ctx, opts, token, clientId) {
 		}
 		throw new Error(error);
 	}
-	if (!jwtPayload.azp) return { active: false };
-	const client = await getClient(ctx, opts, jwtPayload.azp);
+	const tokenClientId = jwtPayload.client_id ?? jwtPayload.azp;
+	if (typeof tokenClientId !== "string") return { active: false };
+	const client = await getClient(ctx, opts, tokenClientId);
 	if (!client || client?.disabled) return { active: false };
-	if (clientId && jwtPayload.azp !== clientId) return { active: false };
+	if (clientId && tokenClientId !== clientId) return { active: false };
 	const sessionId = jwtPayload.sid;
 	if (sessionId) {
 		const session = await ctx.context.adapter.findOne({
@@ -861,7 +1001,8 @@ async function validateJwtAccessToken(ctx, opts, token, clientId) {
 		});
 		if (!session || session.expiresAt < /* @__PURE__ */ new Date()) jwtPayload.sid = void 0;
 	}
-	jwtPayload.client_id = jwtPayload.azp;
+	jwtPayload.client_id = tokenClientId;
+	delete jwtPayload.azp;
 	jwtPayload.active = true;
 	return jwtPayload;
 }
@@ -1167,6 +1308,7 @@ async function rpInitiatedLogoutEndpoint(ctx, opts) {
 			}]
 		});
 	} catch {}
+	deleteSessionCookie(ctx);
 	if (post_logout_redirect_uri) {
 		if (client.postLogoutRedirectUris?.includes(post_logout_redirect_uri)) {
 			const redirectUri = new URL(post_logout_redirect_uri);
@@ -1658,7 +1800,8 @@ const adminCreateOAuthClient = (opts) => createAuthEndpoint("/admin/oauth2/creat
 		grant_types: z.array(z.enum([
 			"authorization_code",
 			"client_credentials",
-			"refresh_token"
+			"refresh_token",
+			deviceCodeGrantType
 		])).default(["authorization_code"]).optional(),
 		response_types: z.array(z.enum(["code"])).default(["code"]).optional(),
 		type: z.enum([
@@ -1842,7 +1985,8 @@ const createOAuthClient = (opts) => createAuthEndpoint("/oauth2/create-client",
 		grant_types: z.array(z.enum([
 			"authorization_code",
 			"client_credentials",
-			"refresh_token"
+			"refresh_token",
+			deviceCodeGrantType
 		])).default(["authorization_code"]).optional(),
 		response_types: z.array(z.enum(["code"])).default(["code"]).optional(),
 		type: z.enum([
@@ -1944,7 +2088,8 @@ const createOAuthClient = (opts) => createAuthEndpoint("/oauth2/create-client",
 							enum: [
 								"authorization_code",
 								"client_credentials",
-								"refresh_token"
+								"refresh_token",
+								deviceCodeGrantType
 							]
 						},
 						description: "Requested authentication method for the token endpoint"
@@ -2044,7 +2189,8 @@ const adminUpdateOAuthClient = (opts) => createAuthEndpoint("/admin/oauth2/updat
 			grant_types: z.array(z.enum([
 				"authorization_code",
 				"client_credentials",
-				"refresh_token"
+				"refresh_token",
+				deviceCodeGrantType
 			])).optional(),
 			response_types: z.array(z.enum(["code"])).optional(),
 			type: z.enum([
@@ -2086,7 +2232,8 @@ const updateOAuthClient = (opts) => createAuthEndpoint("/oauth2/update-client",
 			grant_types: z.array(z.enum([
 				"authorization_code",
 				"client_credentials",
-				"refresh_token"
+				"refresh_token",
+				deviceCodeGrantType
 			])).optional(),
 			response_types: z.array(z.enum(["code"])).optional(),
 			type: z.enum([
@@ -2639,6 +2813,10 @@ const oauthProviderSchema = {
 		scopes: {
 			type: "string[]",
 			required: true
+		},
+		resources: {
+			type: "string[]",
+			required: false
 		}
 	} },
 	/**
@@ -2800,7 +2986,8 @@ const oauthProvider = (options) => {
 		grantTypes: [
 			"authorization_code",
 			"client_credentials",
-			"refresh_token"
+			"refresh_token",
+			deviceCodeGrantType
 		],
 		...options,
 		scopes: Array.from(scopes),
@@ -2863,6 +3011,7 @@ const oauthProvider = (options) => {
 			request_uri: z.string().optional(),
 			code_challenge: z.string().optional(),
 			code_challenge_method: z.enum(["S256"]).optional(),
+			resource: z.union([z.string(), z.array(z.string())]).optional(),
 			nonce: z.string().optional(),
 			prompt: z.enum([
 				"none",
@@ -3152,11 +3301,13 @@ const oauthProvider = (options) => {
 					grant_type: z.enum([
 						"authorization_code",
 						"client_credentials",
-						"refresh_token"
+						"refresh_token",
+						deviceCodeGrantType
 					]),
 					client_id: z.string().optional(),
 					client_secret: z.string().optional(),
 					code: z.string().optional(),
+					device_code: z.string().optional(),
 					code_verifier: z.string().optional(),
 					redirect_uri: SafeUrlSchema.optional(),
 					refresh_token: z.string().optional(),
@@ -3174,11 +3325,12 @@ const oauthProvider = (options) => {
 								properties: {
 									grant_type: {
 										type: "string",
-										enum: [
-											"authorization_code",
-											"client_credentials",
-											"refresh_token"
-										],
+									enum: [
+										"authorization_code",
+										"client_credentials",
+										"refresh_token",
+										deviceCodeGrantType
+									],
 										description: "OAuth2 grant type"
 									},
 									client_id: {
@@ -3193,6 +3345,10 @@ const oauthProvider = (options) => {
 										type: "string",
 										description: "Authorization code (for authorization_code grant)"
 									},
+									device_code: {
+										type: "string",
+										description: "Device code (for device_code grant)"
+									},
 									code_verifier: {
 										type: "string",
 										description: "PKCE code verifier (for authorization_code grant)"
@@ -3600,7 +3756,8 @@ const oauthProvider = (options) => {
 					grant_types: z.array(z.enum([
 						"authorization_code",
 						"client_credentials",
-						"refresh_token"
+						"refresh_token",
+						deviceCodeGrantType
 					])).default(["authorization_code"]).optional(),
 					response_types: z.array(z.enum(["code"])).default(["code"]).optional(),
 					type: z.enum([
@@ -4083,12 +4240,14 @@ function authServerMetadata(ctx, opts, overrides) {
 		registration_endpoint: overrides?.dynamic_client_registration_supported ? `${baseURL}/oauth2/register` : void 0,
 		introspection_endpoint: `${baseURL}/oauth2/introspect`,
 		revocation_endpoint: `${baseURL}/oauth2/revoke`,
+		...overrides?.grant_types_supported?.includes(deviceCodeGrantType) ? { device_authorization_endpoint: `${baseURL}/device/code` } : {},
 		response_types_supported: overrides?.grant_types_supported && !overrides.grant_types_supported.includes("authorization_code") ? [] : ["code"],
 		response_modes_supported: ["query"],
 		grant_types_supported: overrides?.grant_types_supported ?? [
 			"authorization_code",
 			"client_credentials",
-			"refresh_token"
+			"refresh_token",
+			deviceCodeGrantType
 		],
 		token_endpoint_auth_methods_supported: [
 			...overrides?.public_client_supported ? ["none"] : [],
diff --git a/dist/oauth-BFBataE_.d.mts b/dist/oauth-BFBataE_.d.mts
index 21db9f6..753122a 100644
--- a/dist/oauth-BFBataE_.d.mts
+++ b/dist/oauth-BFBataE_.d.mts
@@ -772,6 +772,25 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
     /** oAuthClient metadata */
     metadata?: Record<string, any>;
   }) => Awaitable<Record<string, any>>;
+  /**
+   * Attenuate the scopes used for access-token signing, persistence, refresh,
+   * introspection, and the token response. Returned scopes that were not in
+   * the original grant are ignored.
+   */
+  filterAccessTokenScopes?: (info: {
+    /** The user object if token is associated to a user. */
+    user?: (User & Record<string, unknown>) | null;
+    /** Scopes granted before application-specific attenuation. */
+    scopes: Scopes;
+    /** The resource requesting. */
+    resource?: string;
+    /** Reference of the consent/authorization. */
+    referenceId?: string;
+    /** OAuth client metadata. */
+    metadata?: Record<string, any>;
+    /** Grant type being processed. */
+    grantType: GrantType;
+  }) => Awaitable<Scopes>;
   /**
    * Custom fields to include in the token response body.
    *
@@ -1323,7 +1342,9 @@ interface OAuthRefreshToken<Scopes extends readonly Scope[] = InternallySupporte
   /**
    * The time the user originally authenticated.
    * Persisted so refreshed ID tokens can include a correct `auth_time` claim.
    */
   authTime?: Date;
+  /** Resource indicators bound to the complete original authorization grant. */
+  resources?: string[];
   /**
    * Scopes granted for this refresh token.
@@ -1349,7 +1370,7 @@ type OAuthConsent<Scopes extends readonly Scope[] = InternallySupportedScopes[]>
 /**
  * Supported grant types of the token endpoint
  */
-type GrantType = "authorization_code" | "client_credentials" | "refresh_token";
+type GrantType = "authorization_code" | "client_credentials" | "refresh_token" | "urn:ietf:params:oauth:grant-type:device_code";
 type AuthMethod = "client_secret_basic" | "client_secret_post";
 type TokenEndpointAuthMethod = AuthMethod | "none";
 type BearerMethodsSupported = "header" | "body";
@@ -1379,6 +1398,12 @@ interface AuthServerMetadata {
    * @default `/oauth2/token`
    */
   token_endpoint: string;
+  /**
+   * The URL of the OAuth 2.0 Device Authorization endpoint.
+   *
+   * @default `/device/code`
+   */
+  device_authorization_endpoint?: string;
   /**
    * The URL of the jwks_uri endpoint.
    *
diff --git a/dist/utils-CZyscB8_.mjs b/dist/utils-CZyscB8_.mjs
index ff135a7..b22523a 100644
--- a/dist/utils-CZyscB8_.mjs
+++ b/dist/utils-CZyscB8_.mjs
@@ -261,8 +261,17 @@ function basicToClientCredentials(authorization) {
 			error_description: "invalid authorization header format",
 			error: "invalid_client"
 		});
-		const id = decoded.slice(0, separatorIndex);
-		const secret = decoded.slice(separatorIndex + 1);
+		let id;
+		let secret;
+		try {
+			id = decodeURIComponent(decoded.slice(0, separatorIndex).replaceAll("+", " "));
+			secret = decodeURIComponent(decoded.slice(separatorIndex + 1).replaceAll("+", " "));
+		} catch {
+			throw new APIError$1("BAD_REQUEST", {
+				error_description: "invalid authorization header format",
+				error: "invalid_client"
+			});
+		}
 		if (!id || !secret) throw new APIError$1("BAD_REQUEST", {
 			error_description: "invalid authorization header format",
 			error: "invalid_client"
@@ -278,14 +287,14 @@ function basicToClientCredentials(authorization) {
 *
 * A client's registered `grantTypes` defaults to the documented default
 * `["authorization_code"]` when unset (see client registration). Refresh tokens
-* are only ever issued through the authorization_code flow, so a client allowed
-* to use `authorization_code` is implicitly allowed to use `refresh_token`.
+* are issued through authorization_code or device_code flows, so clients allowed
+* to use either interactive grant are implicitly allowed to use `refresh_token`.
 *
 * @internal
 */
 function clientAllowsGrant(client, grantType) {
 	const allowedGrants = client.grantTypes && client.grantTypes.length > 0 ? client.grantTypes : ["authorization_code"];
-	if (grantType === "refresh_token" && allowedGrants.includes("authorization_code")) return true;
+	if (grantType === "refresh_token" && (allowedGrants.includes("authorization_code") || allowedGrants.includes("urn:ietf:params:oauth:grant-type:device_code"))) return true;
 	return allowedGrants.includes(grantType);
 }
 /**
